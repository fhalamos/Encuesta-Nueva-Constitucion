<p id="notice"><%= notice %></p>

<h1>Listing Quadrants</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Title</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @quadrants.each do |quadrant| %>
      <tr>
        <td><%= quadrant.name %></td>
        <td><%= quadrant.title %></td>
        <td><%= quadrant.description %></td>
        <td><%= link_to 'Show', quadrant %></td>
        <td><%= link_to 'Edit', edit_quadrant_path(quadrant) %></td>
        <td><%= link_to 'Destroy', quadrant, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Quadrant', new_quadrant_path %>
